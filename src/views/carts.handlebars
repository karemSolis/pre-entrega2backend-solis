<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Carrito de Compras</title>
</head>
<body>
    <h1>Carrito de Compras</h1>
    <div class="cart-details">
        <h2>Detalles del Carrito</h2>
        <ul>
            {{#each products}}
                <li>
                    {{this.products}} - Precio: ${{this.valor}} - Cantidad: {{this.quantity}}
                    <button class="remove-from-cart" data-productid="{{this._id}}">Eliminar</button>
                </li>
            {{/each}}
        </ul>
    </div>
    <a href="/">Volver a la lista de productos</a>

</body>
</html>
